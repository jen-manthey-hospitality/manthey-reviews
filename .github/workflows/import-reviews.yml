name: Import Historical Reviews

on:
  workflow_dispatch:  # Manual trigger only

jobs:
  import:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        run: npm install

      - name: Run import
        env:
          SUPABASE_URL: https://ofgicptdoygttkyndrnb.supabase.co
          SUPABASE_ANON_KEY: sb_publishable_CSaPz3PkfOn6eEUpBoWxWQ_l7IaTzAK
        run: node import-nashville-reviews.js

      - name: Import complete
        run: echo "✓ Historical reviews imported to Supabase"
